// Portfolio scripts
console.log('Portfolio loaded 👋');

// Example: highlight nav link of the section currently in view
document.querySelectorAll('a[href^="#"]').forEach((link) => {
  link.addEventListener('click', () => {
    // room for custom behavior
  });
});
